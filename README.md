# Hello!!

### Install
```shell
composer require akceli/laravel-code-generator
php artisan akceli --help
php artisan akceli:publish

# This will generate your first generator, complete with some examples for rapid tweaking.
php artisan akceli:generate generator
```
